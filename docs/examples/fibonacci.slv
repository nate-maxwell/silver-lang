let print = import("io").print
let core = import("core")

let fib = fn(n: int) int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

for i in core.range(1, 10) { print(fib(i)) }
